<div class="row">
  <div class="span8" id = "form_new">
    <%= form_for(@activite) do |f| %>
  <% if @activite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activite.errors.count, "error") %> prohibited this activite from being saved:</h2>

      <ul>
      <% @activite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :activité %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :Contenu %>
    <%= f.text_area :content %> 
  </div>
  <div class="actions">
    <%= f.submit "Créer l'activité" %>
  </div>
<% end %>
  </div>
</div>


